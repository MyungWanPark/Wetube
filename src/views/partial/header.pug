header 
        a(href="/").header__logo
            i.fab.fa-youtube

        nav 
            ul 
                li
                    a(href="/search")
                        i.fas.fa-search
                if loggedIn
                    li
                        a(href="/video/upload") upload video
                    li 
                        a(href="/user/edit") edit profile
                    li 
                        a(href="/user/logout") log out
                    li
                        a(href=`/user/${loggedInUser._id}`)
                            if loggedInUser.avatar_url === ""
                                span 😀
                            else
                                img.header__avatar(src=loggedInUser.avatar_url, crossorigin)
        
                else
                    li
                        a(href="/login") login
                    li
                        a(href="/join").header__btn  Join