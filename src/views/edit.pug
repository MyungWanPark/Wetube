extends layout

block content 
    h4 change title of video
    form(method="POST")
        input(name="title" value=video.title placeholder="type a title here" required)
        input(type="submit" value="Save") 